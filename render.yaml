services:
  - type: web
    name: qa-calendar
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn --chdir app app:app --bind 0.0.0.0:$PORT
    envVars:
      - key: PYTHON_VERSION
        value: "3.11.0"
